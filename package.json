{
  "name": "@fdmg/css-grid",
  "version": "1.0.8",
  "description": "FDMG CSS Grid with Flexbox fallback",
  "style": "dist/css/grid.css",
  "scripts": {
    "build": "npm run clean && npm run lint && tsc && webpack --mode development && npm run sass",
    "compile-release": "tsc && webpack --mode production",
    "clean": "rimraf dist",
    "mkdir": "mkdirp dist/css",
    "lint": "tslint -p .",
    "release": "npm update && npm install && yarn && npm run clean && npm run lint && npm run compile-release && npm run sass",
    "sass": "npm run sass-grid && npm run sass-flex",
    "sass-grid": "npm run mkdir && node-sass --output-style=compressed scss/grid.scss -o dist/css",
    "sass-flex": "npm run mkdir && node-sass --output-style=compressed scss/flex.scss -o dist/css",
    "stop": "pm2 stop ecosystem.config.js",
    "start": "pm2 start ecosystem.config.js --watch",
    "test": "echo \"No tests specified\" && exit 0",
    "watch-client": "webpack --mode development --watch",
    "watch-server": "tsc --watch"
  },
  "author": "Willem Liu",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git@github.com:FDMediagroep/fdmg-css-grid.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/react": "^16.0.36",
    "@types/react-dom": "^16.0.3",
    "awesome-typescript-loader": "^5.0.0",
    "fs-extra": "^5.0.0",
    "koa": "^2.4.1",
    "koa-compress": "^2.0.0",
    "koa-json": "^2.0.2",
    "koa-mount": "^3.0.0",
    "koa-router": "^7.4.0",
    "koa-static": "^4.0.2",
    "mkdirp": "^0.5.1",
    "node-sass": "^4.7.2",
    "path-to-regexp": "^2.1.0",
    "pm2": "^2.10.1",
    "preact": "^8.2.7",
    "preact-compat": "^3.18.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.9.1",
    "tslint-react": "^3.4.0",
    "typescript": "^2.7.1",
    "webpack": "^4.0.1",
    "webpack-cli": "^2.0.9"
  },
  "dependencies": {}
}
